@if(Request.IsAuthenticated) {
    @Html.ActionLink(User.Identity.Name, "Index", "Login", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
else {
    @Html.ActionLink("Log in", "Login", "Login", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
