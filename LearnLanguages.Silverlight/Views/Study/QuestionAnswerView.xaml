<UserControl xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"  x:Class="LearnLanguages.Silverlight.Views.QuestionAnswerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
  <Border Style="{StaticResource DebugBorder}">
    <Grid x:Name="LayoutRoot" Background="White">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Border Grid.Row="0" Style="{StaticResource ViewSection}">
        <TextBlock x:Name="Question_Text" Visibility="{Binding Path=QuestionVisibility}" />
      </Border>
      
      <Border Grid.Row="1" Style="{StaticResource ViewSection}">
        <toolkit:BusyIndicator IsBusy="{Binding Path=HidingQuestion}">
          <TextBlock x:Name="Answer_Text" Visibility="{Binding Path=AnswerVisibility}" />
        </toolkit:BusyIndicator>
      </Border>
    </Grid>
  </Border>
</UserControl>
